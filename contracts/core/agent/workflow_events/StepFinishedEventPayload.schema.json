{
  "$id": "https://inkforge.local/contracts/agent/workflow_events/StepFinishedEventPayload/2.0",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "errorCode": {
      "anyOf": [
        {
          "maxLength": 128,
          "minLength": 1,
          "pattern": "^[A-Z][A-Z0-9_]*$",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "title": "Errorcode"
    },
    "fencingToken": {
      "exclusiveMinimum": 0,
      "title": "Fencingtoken",
      "type": "integer"
    },
    "status": {
      "enum": [
        "completed",
        "failed",
        "skipped"
      ],
      "title": "Status",
      "type": "string"
    },
    "stepId": {
      "maxLength": 128,
      "minLength": 1,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]*$",
      "title": "Stepid",
      "type": "string"
    }
  },
  "required": [
    "stepId",
    "fencingToken",
    "status",
    "errorCode"
  ],
  "title": "StepFinishedEventPayload",
  "type": "object",
  "x-inkforge-python-type": "inkforge_contracts.workflow_events.StepFinishedEventPayload"
}
