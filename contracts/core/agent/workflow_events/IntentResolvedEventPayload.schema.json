{
  "$id": "https://inkforge.local/contracts/agent/workflow_events/IntentResolvedEventPayload/2.0",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "confidence": {
      "maximum": 1,
      "minimum": 0,
      "title": "Confidence",
      "type": "number"
    },
    "operation": {
      "maxLength": 128,
      "minLength": 1,
      "pattern": "^[a-z][a-z0-9_.-]*$",
      "title": "Operation",
      "type": "string"
    },
    "targetId": {
      "maxLength": 128,
      "minLength": 1,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]*$",
      "title": "Targetid",
      "type": "string"
    },
    "targetType": {
      "maxLength": 128,
      "minLength": 1,
      "pattern": "^[a-z][a-z0-9_.-]*$",
      "title": "Targettype",
      "type": "string"
    },
    "workflow": {
      "maxLength": 128,
      "minLength": 1,
      "pattern": "^[a-z][a-z0-9_.-]*$",
      "title": "Workflow",
      "type": "string"
    }
  },
  "required": [
    "workflow",
    "operation",
    "targetType",
    "targetId",
    "confidence"
  ],
  "title": "IntentResolvedEventPayload",
  "type": "object",
  "x-inkforge-python-type": "inkforge_contracts.workflow_events.IntentResolvedEventPayload"
}
