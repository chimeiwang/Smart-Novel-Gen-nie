export function buildApplyStyleBody(
  styleId: string | null,
  expectedStyleId: string | null,
): { styleId: string | null; expectedStyleId: string | null } {
  return { styleId, expectedStyleId };
}
