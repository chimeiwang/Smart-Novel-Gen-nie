> 状态：历史归档，不作为当前实现依据。当前事实以 `DOCS.md`、`AGENTS.md`、`src/agents/AGENTS.md`、代码和 schema 为准。

# 5.2 Agent工具调用

**状态**: ✅ (v5.3.1 统一 max_tokens)

## 版本
### v5.3.1 (2026-06-05)
- 新增 MAX_OUTPUT_TOKENS = 384000（DeepSeek V4 上限）
- callLLM / callLLMSync / callLLMWithTools 统一添加显式 max_tokens 参数
- 删除过时的 TOOL_RESULT_MAX_CHARS JSDoc 注释
- 变更原因：a916be3 删除 max_output_tokens 后回退到 API 默认 4096，导致输出截断

### v5.3 (2026-06-05)
- 流式工具调用，并行执行(Promise.all)
- callLLMWithTools添加LangSmith追踪
- 已移除 Tool Result Budget 截断 + max_output_tokens 限制（a916be3）

### v5.2 (2026-06-03)
- 初始实现，按Agent角色分组工具
- 设定顾问11工具 / 剧情顾问9工具 / 校验员7工具

## 代码
- tools.ts: LORE_TOOLS, PLOT_TOOLS, VALIDATOR_TOOLS, executeToolCall()
- llm-wrapper.ts: callLLMWithTools()
- langsmith-tracer.ts: traceAgentExecution()

## 注意
- 不同Agent看到不同工具列表，防止越权调用
- LLM可能一次请求多个tool call，需全部执行后再调LLM
- 工具返回结果需控制大小
