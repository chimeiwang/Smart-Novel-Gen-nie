# 正文各项 token 预算统一验收

日期：2026-09-11。状态：本地验收完成，等待完整 CI 与生产发布。规格见
[各项 token 预算统一](../specs/2026-09-11-chapter-writing-token-budgets.md)。

## 已完成验证

- 共享契约先证明两个旧校验拒绝独立分项上限及 disabled 正额度，修正后全部 841 项契约测试通过。
- Agent execution 全目录 651 passed、1 skipped；补充输出边界及复审关闭推理断言后，正文测试文件 49 passed。
- Registry 与架构资产检查合计 32 passed；保留其他操作的历史冻结校验，只归一化本次获批的正文额度。
- Java Step、Run、Registry、执行快照四个测试类共 48 项通过；Windows 根 Maven verify 在已有私钥符号链接权限测试阻断，完整构建以本次 Linux CI 为准。
- Ruff 全仓通过，Mypy 294 个源码文件通过，派生资产无漂移，diff 空白检查通过。
- v3 三个 Step 五项 token 上限均为 100,000，Run 五项累计额度均为 600,000。
  资产指纹：`f554365c8c4b5bd358110a7e0bb7754ee98ca9ba5ee7d67e449f36d36379eebe`。
- 实际生产失败用量 31,742 / 8,627 / 11,406 / 2,779 的合法响应可通过；总输出 100,001 或两分项合计 200,000 均拒绝。
  这些是 Fake Provider 回归，不代表已经在生产发起新正文任务。
- 新发布前只读核验生产仍为 `5936bf38`、服务健康、route=all、非终态 V2 Run 为零。
  本次涉及的四个 Java 源码/测试目录错误归属已逐目录恢复为原部署用户，Git 引用、对象与锁权限验证通过。

## 发布边界

生产已有 v1/v2 Run 的预算不修改。继续使用独立热修复分支，保留生产数据库、视频开关和原模型策略。
上次 GitHub 34577596386 的 deploy 失败记录没有被手动部署改写，本次需以新的 CI/deploy 作业结论和线上读回分别验收。
