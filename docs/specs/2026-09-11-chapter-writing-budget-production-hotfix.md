# 正文输入预算独立生产热修复

日期：2026-09-11。状态：执行中。用户已明确要求将正文 100,000 输入预算上线。

## 发布范围

生产当前运行 `0a7e7017`。主分支后续 `f156a8a9` 包含未迁入生产的视频结构改动，且旧 Java 视频
契约引用导致干净 CI 编译失败。生产视频迁移未获授权，本次不将这些改动一并发布。

- 从 `0a7e7017` 建立 `codex/chapter-input-budget-release`，仅移植正文输入预算、旧任务预算兼容、
  对应测试及说明；主分支和用户未提交内容保持不变。
- 正文生成与双复审采用 v2 单步输入／冷缓存 100,000，Run 累计两项均为 600,000；保留全部 v1 预算。
- 热修复分支使用原 `CI and Deploy` 全套 CI、production environment、串行部署锁、镜像与源码
  提交绑定及既有健康／回滚检查。仅将该确切分支加入 push/deploy 触发，部署 BRANCH 绑定触发分支。
- 不变更数据库契约、迁移、Compose、服务器密钥或视频开关；生产继续关闭视频，禁止执行 DDL。

## 验收与生效

1. 比较生产基线，确认无数据库／公共接口／视频功能改动；执行资产只有预算与对应哈希改变。
2. 热修复分支 CI 全部通过后才构建、上传、部署；不使用本机旧 target 产物证明干净构建。
3. 部署后核对三服务镜像 SHA、健康状态、执行资产指纹和正文实际预算；新 Run 才采用新预算。
4. 已有 Run 保持冻结预算与恢复能力，不擅自重试创作任务、修改小说成果或伪造新模型调用。

## 提交前证据

- 本地相关测试 96 passed；Compose 权限位测试在 Windows 的 POSIX mode 断言失败，留给 Linux CI 原样验证。
- Ruff 全仓通过，Mypy 294 个源码文件通过；执行资产派生检查及 diff 检查通过。
- 根 Maven verify 在 Windows 符号链接权限测试失败，未进入 Core；不将本地结果视为完整构建通过。
- 数据库契约、公共 OpenAPI 和 Compose 与生产基线完全一致。预算资产指纹为
  `8f98910764258f638d629d972452b2131af572e8c7e6411038e142a84df99db6`。
- 首轮 Linux CI 已完成干净 Java 编译，Core 1209 项测试中两项旧超额测试数据因预算扩大不再超额。
  现从持久化的冻结预算读取基准并使用超限 1 的用量，保留原子拒绝断言；本地全部 Java 测试源码编译通过，
  重新提交完整 CI 验证，不把首轮失败记成通过。
