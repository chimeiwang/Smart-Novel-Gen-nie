# 5.1 Agent核心编排（StateGraph）

**状态**: ✅ (v5.3 五Agent架构)

## 版本
### v5.3 (2026-06-05)
- 新增编辑Agent，五Agent架构
- MAX_CALL_CHAIN_DEPTH: 5→20

### v5.2 (2026-06-03)
- LLM智能路由 + 分层上下文 + JSON结构化输出

### v5.1 (2026-05-30)
- 人工确认 + 失败重试(retryPolicy) + 增量更新

### v5.0 (2026-05-09)
- 引入LangGraph StateGraph替代手动递归调度
- processResult节点统一后处理

## 代码
- executor.ts: runWritingWorkflow(), buildGraph(), createInitialState(), agentNode(), initSessionNode, processResultNode
- state.ts: WritingState, CORE_AGENT_IDS, AGENT_NAMES
- context-manager.ts: addUserMessage(), addAgentMessage(), serializeHistory()
- response-parser.ts: parseAgentResponse()
- nodes/index.ts: importAgentNode()

## 入口
- `/api/writing/session` → runWritingWorkflow()
- `/api/writing/resume` → resumeWritingWorkflow()

## 注意
- agentNode工厂不能try-catch，否则retryPolicy失效
- MemorySaver单例，进程重启丢失会话
- Agent 不再通过动态路由工具互相转交；审核/返工循环由 LangGraph `operationWorkflow` 的边和迭代上限控制。
