# 5.4 分层上下文

**状态**: ✅ (index模式 + full模式)

## 版本
### v5.2 (2026-06-03)
- buildContextIndex() ~200 token 供查询型Agent
- buildNovelContext() ~5000 token 供生成型Agent
- ContextBuildOptions 15个选项控制

### v5.0 (2026-05-09)
- 提取共享context-builder.ts，消除4个Agent中800行重复代码

## 代码
- context-builder.ts: buildNovelContext(), buildContextIndex(), ContextBuildOptions
- context-aggregator.ts: aggregateNovelContextLightweight()

## 注意
- 工具调用型Agent用索引（自己查），纯生成型用完整上下文
- ContextBuildOptions默认全false，调用方须显式指定
- SQLite本地查询足够快，数据量大时需关注性能
